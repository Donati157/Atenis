node_modules/
.next/
.env
.env.local
.env*.local
.DS_Store
next-env.d.ts
*.tsbuildinfo
.vercel

# Local tooling / IDE / harness — não pertence ao repo
.claude/
.tools/
# skills/ é doc humana de voz e método do Atenis — fonte canônica em lib/voice.ts
# (entra no repo; ignorado só no .vercelignore pra não inchar o build).

# Pastas de outros projetos que moram aqui localmente
Google Chrome.app/
Edu Iceberg/
calendario-jose/

# Soltos
poster-china.html
