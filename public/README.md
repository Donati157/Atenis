# Assets públicos

- `logo.svg` — logo placeholder do Atenis (iceberg estilizado).
  Substitua pelo logo oficial quando tiver. Se quiser usar um `.jpeg`/`.png`,
  troque as referências `src="/logo.svg"` em `app/page.tsx`,
  `components/auth-shell.tsx` e `components/chat-dashboard.tsx`.
